{
  "name": "@cortexide/core",
  "version": "0.1.0",
  "description": "Cortexide core orchestrator and phase execution engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "@open-policy-agent/opa-wasm": "^1.8.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.10",
    "ts-jest": "^29.1.1"
  }
}
